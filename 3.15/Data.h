#include <iosteam>

using namespace std;

class Data
{
public:
    Data(int a,int b,int c)
    {
        setMonth(int a)
        setDat
        if(a >= 1 && a <= 12)
        {
            Month = a;
        }
        else
            Month = 1;
    }
};
